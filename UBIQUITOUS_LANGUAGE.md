# 智能排障统一语言

本文件约束面向开发、值班人员和服务负责人的产品文案。内部字段名可以保持兼容，用户界面必须优先表达“发生了什么”，再按需展示技术口径。

## 取证事实

| 统一用语 | 定义 | 避免使用 |
| --- | --- | --- |
| **失败请求** | 故障窗口内满足失败条件的一次独立请求。 | 失败样本、失败日志 |
| **正常请求** | 同一比较窗口内满足正常条件的一次独立请求。 | 成功样本、基线样本 |
| **异常特征** | 查询规则要在失败请求与正常请求中比较的可复核特征。 | 区分特征、discriminating feature |
| **出现异常特征** | 某个请求包含当前规则检查的异常特征。 | 命中 |
| **关联日志** | 同一个关联 ID 下按时间排序并安全压缩的日志记录。 | 链路节点、调用链 hop |
| **异常日志** | 日志级别或安全白名单关键词将其标记为异常的关联日志。 | 根因节点、故障服务 |
| **服务分布** | 关联日志中记录的服务及其出现次数。 | 完整调用路径 |
| **受影响数量** | 有证据引用和观测时间支持的客户数或用户数。 | 告警次数、日志条数 |

## 判断结果

| 统一用语 | 定义 | 避免使用 |
| --- | --- | --- |
| **排障方法已选定** | 告警已选择一个审核通过的排障方法。 | Playbook 命中、命中路 |
| **判断条件成立** | 当前证据满足一条确定性判断条件。 | 判据命中、规则 fired |
| **已定位** | 审核规则要求的必要证据均成立，系统可以指出异常环节。 | 已找到绝对根因、100% 确定 |
| **根因假设** | 当前证据支持一个待人工确认的可能原因。 | 已定位、已证明 |
| **证据不足** | 必需证据缺失或不可用，系统停止自动判断。 | 没有问题、查询结果正常 |
| **结论可信程度** | 系统根据调查路径权威、证据完整性和冻结规则给出的等级，不是模型自报概率。 | AI 置信度、正确率 |

## 时间与评估

| 统一用语 | 定义 | 避免使用 |
| --- | --- | --- |
| **本次取证用时** | 读取本次真实证据所消耗的应用侧时间。 | 观测云执行耗时、总排障耗时 |
| **形成结论用时** | 从进入调查到首次形成结论或明确停止的时间。 | 本次取证用时 |
| **人工处置用时** | 从结论可供人工处理到人工采纳、转派或关闭的时间。 | 系统诊断耗时 |
| **真实案例** | 来自真实数据源且关联到真实 Diagnosis 的排障记录。 | 演示样本、Replay 样本 |
| **历史回放案例** | 使用冻结的历史证据验证流程稳定性的记录。 | 真实在线案例 |

## 数字表达规则

1. **禁止只显示斜杠比例。** 不写“失败 2/2，成功 0/36”，统一写成：
   > 2 个失败请求都出现了“内容拦截”；36 个正常请求均未出现。
2. **先显示人数或请求数，再显示比例。** 百分比只放在展开的技术详情中。
3. **必须说清统计对象。** 明确是请求、日志、用户、客户还是排障案例，不能只写“2 条”或“36 个”。
4. **必须说清比较特征。** 不写“命中特征”，写“出现了内容拦截”或对应场景的真实特征名称。
5. **小规模事实保持原样。** 写“目前只观察到 2 个失败请求”，不把有限样本包装成普遍规律。
6. **缺失不是零。** 未取到数据统一显示“没有取得可用数据”，不能显示为 `0` 或“正常”。
7. **日志事件不是服务调用。** 关联日志数量不能命名为调用链节点数。
8. **等级必须解释依据。** `HIGH / MEDIUM / LOW` 对外显示为“结论依据充分 / 结论依据有限 / 仅供人工核查”，并说明来自规则和证据完整性，不显示成概率。
9. **先规范化再翻译。** API 为保护 64 位整数精度而返回的数字字符串，必须先转成计数再生成文案；字符串 `"0"` 仍应显示为“均未出现”，不能落入普通非零分支。

## 关系

- 一个 **失败请求** 可以对应多条 **关联日志**。
- 一个 **正常请求** 也可以出现某些背景特征，因此必须与 **失败请求** 对照。
- **异常特征** 在失败请求中明显出现、在正常请求中很少出现，只能支持 **判断条件成立**；是否达到 **已定位** 仍由审核规则决定。
- **异常日志** 是核查线索，不自动等于根因。
- **排障方法已选定** 不代表 **判断条件成立**，更不代表 **已定位**。

## 示例对话

> **开发：** 页面上的“2/2 命中”是什么意思？
>
> **系统：** 本次找到 2 个失败请求，这 2 个请求都出现了“内容拦截”；同一时间段的 36 个正常请求都没有出现。
>
> **开发：** 那是不是已经证明内容拦截就是根因？
>
> **系统：** 当前审核规则的判断条件已经成立，因此系统给出“已定位”；但目前只观察到 2 个失败请求，仍应由人工确认并记录最终处置结果。

## 已识别的歧义

- “命中”同时被用于日志检索、特征出现、判断条件成立和排障方法选择，必须按具体动作分别表达。
- “样本”没有说明统计对象；当前 ITGW 对照统计的是独立请求，不是日志行。
- “节点”曾指规范化日志事件，容易被理解为微服务或 Trace Span；统一改为“关联日志”。
- `2/2` 与 `36/0` 的分子分母顺序在文档和页面中不一致；用户界面禁止继续使用这种缩写。
- “可信等级 HIGH”容易被理解为模型概率；它实际是系统根据路径、证据和规则投影的等级。
- “取证用时”与“形成结论用时”是不同口径，不能互相替代。
